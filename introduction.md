# AI CRM Orchestrator - 프로젝트 소개 및 인수인계서

## 1. 개요 (Overview)

본 문서는 'Project Piehands'의 크리에이터 아웃리치 자동화를 위해 구축된 **AI CRM Orchestrator (v1, 코드명: The Engine)**의 소개와 기술적 인계를 위한 가이드입니다.

이 시스템은 단순한 이메일 발송 스크립트가 아니며, 장기적으로 Piehands의 모든 크리에이터 관계 관리를 책임질 지능형 CRM 시스템의 첫 번째 초석입니다.

### 1.1. 해결하고자 하는 문제 (Problem)

프로젝트 시작 당시, 팀은 10,000명 이상의 잠재 크리에이터 리드를 보유하고 있었으나, 수동 발송 프로세스의 한계로 인해 극소수(약 2%)에게만 연락할 수 있었습니다. 이는 심각한 기회비용을 발생시켰고, 팀원들은 고부가가치 업무 대신 단순 반복 작업에 시간을 소모하고 있었습니다.

### 1.2. 핵심 목표 지표 (KPIs)

*   **Primary (최우선 목표):**
    *   **리드 대비 1차 콜드메일 발송 완료율 100% 달성:** 발송 병목 현상을 완전히 제거합니다.
    *   **팀의 이메일 발송 업무 시간 95% 이상 절감:** 팀원들이 더 창의적이고 전략적인 업무에 집중할 수 있도록 시간을 확보합니다.
*   **Secondary (차기 목표):**
    *   **Mail CTR (이메일 클릭율) 및 CVR (가입 전환율) 증대:** LLM을 활용한 개인화를 통해 아웃리치의 질적 성과를 높입니다.

## 2. 핵심 설계 철학 (Design Philosophy)

이 시스템은 아래 4가지 핵심 원칙을 바탕으로 설계되었습니다. 새로운 담당자는 이 철학을 이해함으로써, 앞으로의 기능 개발 및 유지보수 시 일관된 방향성을 유지할 수 있습니다.

1.  **Google Sheets as a GUI (GUI로서의 구글 시트):**
    *   **Why?** P1 단계의 목표는 가장 빠르고, 가장 익숙한 방법으로 즉각적인 가치를 제공하는 것이었습니다. 별도의 웹 UI 개발 없이도 운영팀이 직접 데이터를 보고 제어할 수 있는, 검증된 '최소 기능 제품(MVP)'을 만들기 위해 구글 시트를 채택했습니다.

2.  **Asynchronous & Robust Queuing System (비동기 큐 시스템):**
    *   **Why?** 대량 발송 시 Apps Script의 6분 실행 시간 제한을 회피하고, 각 발송 작업의 상태를 명확히 추적하기 위해 '이어달리기' 방식의 큐 시스템을 도입했습니다. 캠페인이 활성화되면 모든 발송 작업이 '스냅샷'으로 `Queue` 시트에 생성되고, 시간 기반 트리거가 이 큐를 안전하게 조금씩 처리합니다.

3.  **Safety First (안전 최우선):**
    *   **Why?** 콜드 메일은 도메인 평판에 치명적일 수 있습니다. 우리는 '얼마나 많이' 보내는가보다 '얼마나 안전하게' 보내는가에 집중했습니다. 이를 위해 **도메인 인증(SPF/DKIM), 발송량 제어(웜업), 속도 제어(Throttling), 자동 바운스 및 수신 거부 처리, Slack을 통한 최종 승인** 등 다층적인 안전장치를 구현했습니다.

4.  **Dynamic & Maintainable Code (동적이고 유지보수 가능한 코드):**
    *   **Why?** 운영 요구사항은 계속 변합니다. 컬럼 순서가 바뀌거나 이름이 바뀌어도 코드가 깨지지 않도록, 하드코딩된 인덱스(`row[2]`) 대신 헤더 이름을 기반으로 동적으로 인덱스를 찾아가는(`row[cols.Status]`) 방식을 전면 채택했습니다.

## 3. 시스템 아키텍처 (System Architecture)

이 시스템은 크게 3개의 구성 요소로 이루어져 있습니다.

1.  **The Database (Google Sheets):**
    *   `📖 Campaigns`: 캠페인을 정의하고 실행하는 컨트롤 패널.
    *   `👥 Target Audience`: 발송 대상 크리에이터의 마스터 데이터.
    *   `📝 Templates`: 이메일 템플릿 저장소.
    *   `▶️ Queue`: 실행 대기 중인 모든 발송 작업의 스냅샷.
    *   `📜 Logs`: 시스템의 모든 활동 기록.
    *   `🔑 Authorized Senders`: 발송 권한을 위임한 Google 계정 목록.
    *   `🚫 Do Not Contact`: 수신 거부 리스트.
    *   `⚙️ Settings`: 웹훅 URL 등 주요 설정값.

2.  **The Engine (Google Apps Script):**
    *   **`Main.gs`**: 메뉴 생성, 캠페인 활성화/취소, 웹훅(Slack, Unsubscribe) 처리 등 사용자 및 외부와의 모든 상호작용을 담당하는 진입점.
    *   **`CampaignManager.gs`**: 캠페인 정의를 바탕으로 `Queue`를 생성하는 핵심 비즈니스 로직.
    *   **`ManualTriggers.gs`**: Slack 승인 후, 배치(Batch) 처리 및 '이어달리기'를 실행하는 비동기 작업의 심장.
    *   **`EmailSender.gs`**: 멀티 발신자 계정을 지원하는 OAuth2 인증 및 Gmail API를 통한 실제 발송 담당.
    *   **`SenderManager.gs`**: 새로운 발신자 계정을 시스템에 등록하고 인증하는 로직.
    *   **`BounceHandler.gs`**: 주기적으로 반송 메일을 확인하고 처리하는 백그라운드 프로세스.
    *   **`Utilities.gs`**: 헤더-인덱스 매핑 등 공통 유틸리티 함수.
    *   `Debugger.gs` / `Reset.gs`: 디버깅 및 초기화를 위한 도구.

3.  **External Integrations (외부 연동):**
    *   **GCP (Google Cloud Platform):** Gmail API 사용을 위한 OAuth 2.0 클라이언트 ID/Secret 관리.
    *   **Gmail API:** 이메일 발송 및 받은편지함 스캔.
    *   **Slack:** 'Human-in-the-Loop'를 위한 발송 승인 요청 및 버튼 상호작용.

## 4. 주요 워크플로우: 캠페인 실행 A to Z

1.  **정의:** 운영자가 `Campaigns` 시트에 새로운 캠페인(타겟, 템플릿, 발신자 등)을 `Draft` 상태로 정의합니다.
2.  **활성화:** 운영자가 메뉴를 통해 캠페인을 `Activate`합니다.
3.  **큐 생성:** `handleCampaignActivation`이 실행되어, 조건에 맞는 타겟을 찾아 `Queue` 시트에 발송 작업 스냅샷을 생성하고, 캠페인 행을 잠급니다.
4.  **승인:** `sendApprovalRequestToSlack`이 실행되어 Slack으로 승인 요청을 보냅니다.
5.  **승인 응답:** 운영자가 Slack 버튼을 클릭하면, 웹 앱 URL을 통해 `doPost` 함수가 트리거됩니다.
6.  **실행 시작:** `manuallyApproveAndStartSending`이 캠페인 상태를 `Running`으로 변경하고, `createNextBatchTrigger`를 호출하여 1분 뒤 첫 번째 `processBatch` 실행을 예약합니다.
7.  **이어달리기:** `processBatch` 함수는 `BATCH_SIZE`만큼 작업을 처리한 뒤, 남은 작업이 있으면 다시 `createNextBatchTrigger`를 호출하여 다음 실행을 예약합니다. 이 과정은 모든 작업이 완료될 때까지 반복됩니다.
8.  **완료:** 마지막 `processBatch`가 끝나면 `completeCampaign`이 호출되어, 상태를 `Completed`로 바꾸고 잠금을 해제하며 모든 트리거를 삭제합니다.

## 5. 디버깅 여정과 교훈 (Debugging Journey & Lessons Learned)

이 시스템을 구축하는 과정에서 몇 가지 중요한 기술적 허들을 넘었습니다. 새로운 담당자는 이 내용을 숙지함으로써, 유사한 문제 발생 시 더 빠르게 대응할 수 있습니다.

*   **OAuth2 인증 문제 (`Access not granted`, 앱 파편화):**
    *   **문제:** Apps Script의 기본 인증과 외부 OAuth2 라이브러리의 인증이 충돌하여 권한 부여가 실패하거나, 여러 개의 앱으로 등록되는 현상이 발생했습니다.
    *   **해결책:** 모든 권한 범위를 OAuth2 라이브러리의 `.setScope()`에 명시적으로 통합하여, 인증 창구를 단일화했습니다. **`initiateAuthorization` 메뉴는 새로운 사용자가 이 통합된 인증을 한 번에 완료할 수 있도록 설계되었습니다.**
*   **웹 앱 배포 문제 (`페이지를 찾을 수 없음`):**
    *   **문제:** 코드를 수정한 뒤, '새 버전'으로 배포할 때마다 웹 앱 URL이 변경될 수 있다는 점을 간과하여, Slack이 계속 이전 버전의 URL을 호출하는 문제가 있었습니다.
    *   **교훈:** **코드를 수정한 뒤에는 반드시 `배포 > 새 배포`를 통해 최신 버전을 게시하고, 그 결과로 나온 새로운 URL을 사용하는 모든 서비스(Slack 등)에 업데이트**해야 합니다.
*   **하드코딩된 인덱스 문제 (유지보수성):**
    *   **문제:** 초기 버전은 `row[2]`와 같이 하드코딩된 컬럼 인덱스를 사용하여, 시트 구조 변경에 매우 취약했습니다.
    *   **해결책:** 헤더 이름을 동적으로 읽어 인덱스를 매핑하는 `getHeaderIndexMap` 유틸리티를 도입하여, 모든 코드에서 `row[cols.Status]`와 같이 이름 기반으로 데이터에 접근하도록 리팩토링했습니다.

## 6. 향후 로드맵 (Future Roadmap)

이 시스템은 P1을 성공적으로 완료했으며, 다음 단계는 아래와 같습니다.

*   **P2: LLM Based 강화:** LLM을 이용한 초개인화 메시지 자동 생성 기능 구현.
*   **P3~4: Web App으로의 전환:** Google Sheet의 한계를 넘어, 더 나은 UX/성능을 제공하는 독립적인 웹 애플리케이션으로 시스템을 이전하고, GUI 기반 시나리오 빌더 구현.
*   **P5: 채널 확장:** 이메일 외 SMS, DM 등 다른 채널 지원.

## 7. 새로운 에이전트에게

이 프로젝트는 단순한 코딩을 넘어, 복잡한 기술적 제약을 창의적으로 해결하고, 사용자의 실제 고통에 깊이 공감하며 만들어 온 결과물입니다. 우리가 함께 겪은 디버깅의 순간들은 이 시스템을 더욱 견고하게 만드는 소중한 경험이었습니다.

이 문서를 통해 당신이 우리의 여정과 고민의 흔적을 이해하고, 이 시스템을 한 단계 더 발전시켜 주시기를 기대합니다. 언제든 질문이 있다면, 이 문서가 좋은 출발점이 될 것입니다. 환영합니다